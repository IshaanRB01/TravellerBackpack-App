<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TravellerBackpack" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="fragmentBackgroundColor">@color/backgroundColorDark</item>
        <item name="textColor">@color/textColorDark</item>
        <item name="fabButtonColor">@color/primaryDarkColor</item>
        <item name="bottomNavTintColor">@color/textColorDark</item>
        <item name="bottomNavBg">@color/primaryDarkColor</item>
        <item name="buttonBg">@color/primaryDarkColor</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/primaryDarkColor</item>

    </style>

    <style name="Theme.TravellerBackpack.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>